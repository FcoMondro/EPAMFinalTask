const CREDENTIALS = {
    EMPTY_CREDENTIALS : {
        email : "",
        password : ""
    },
    VALID_CREDENTIALS : {
        email : "standard_user",
        password: "secret_sauce"
    },
    INVALID_CREDENTIALS : {
        email : "email",
        password : "password"
    }
}

exports.CREDENTIALS = CREDENTIALS;